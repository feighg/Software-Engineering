<link rel='stylesheet' type='text/cess' href='css/style.css' />

<h2>Hello, {{parent}}</h2>

<h4>Choose a task</h4>
<form action='/reactivateTask' method='post'>
    Task Name:
    <select name="taskHistory" id="task-selector" required>
        {{#each tasks}}
            <option value="{{name}}">{{name}}</option>
        {{/each}}
    </select><br>
    Due Date:
    <input type="date" name="date"><br>
    Task Status:<br>
    <input type="checkbox" name="mandatory" value="mandatory">Mandatory<br>
    <input type="checkbox" name="addTaskHistory" value="addTask">Add to task history<br>
    Assign to child:
    <select name="childSelect" id="child-selector" required>
        <option value="">Select Child</option>
        {{#each selection}}
            <option value="{{name}}">{{name}}</option>
        {{/each}}
    </select><br>


    <input type="submit" value="Reactivate Task">
</form>
